from pygame import * 
init ()
screen = display.set_mode((800,600))
clock = time.Clock()

running = True
while running:
  for e in event.get():
    if e.type == QUIT:
      running = False
      
  screen.fill((0,0,0))
  
  display.update()
  clock.tick(60)
